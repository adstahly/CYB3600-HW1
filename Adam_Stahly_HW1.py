import random

plaintext = input("Enter the plaintext message: ")
key_length = random.randint(4, 10)
tmatrix = [len(plaintext)/key_length][key_length]
for char in plaintext:
    tmatrix[]
print(tmatrix)